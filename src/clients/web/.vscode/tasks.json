{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "npm",
			"script": "start",
			"problemMatcher": {
				"pattern": {
					"regexp": "^(?:.+?\\s)?VITE\\s+v([\\d.]+)\\s+ready in (\\d+)\\s+ms",
					"file": 1
				},
				"background": {
					"activeOnStart": true,
					"beginsPattern": "^(?:.+?\\s)?VITE\\s+v([\\d.]+)",
					"endsPattern": "^(?:.+?\\s)?VITE\\s+v([\\d.]+)\\s+ready in (\\d+)\\s+ms"
				}
			},
			"label": "vite:start",
			"detail": "vite",
			"isBackground": true
		}
	]
}